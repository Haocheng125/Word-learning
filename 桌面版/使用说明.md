# 桌面版使用说明

## 📋 当前状态

- ✅ 代码已更新（PDF 读取逻辑已替换为与后端一致的 pdfplumber）
- ✅ 下载功能路由已配置
- ⏳ 桌面版 EXE 需要手动打包

---

## 📦 手动打包步骤

### 1. 打开 PowerShell 并进入桌面版目录
```powershell
cd c:\Users\18896\Desktop\单词网站\桌面版
```

### 2. 安装依赖
```powershell
python -m pip install -r requirements.txt
python -m pip install PyInstaller
```

### 3. 打包（两种方式选一种）

**方式一：简单命令（推荐）**
```powershell
python -m PyInstaller --onefile --windowed --name "单词学习助手" main.py
```

**方式二：使用配置文件**
```powershell
python -m PyInstaller --clean build.spec
```

### 4. 复制到后端目录
```powershell
copy dist\单词学习助手.exe ..\backend\uploads\downloads\
```

### 5. 重启 Docker 后端
```powershell
cd ..
docker compose up -d --build backend
```

---

## 📥 下载地址

打包完成后：
- 前端页面：http://localhost:4000/download
- 直接下载：http://localhost:5000/admin/download/desktop-app
